{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */SKILL.md) python3 \"$CLAUDE_PROJECT_DIR/skills/skill-foundry/scripts/validate_skill.py\" \"$(dirname \"$f\")\" >&2 || exit 2;; esac; }",
            "timeout": 30,
            "statusMessage": "SKILL.md を検証中..."
          }
        ]
      }
    ]
  }
}
