# Claude Code 負債棚卸し（Debt Inventory）

対象: `GHshimon/skills` リポジトリ + 本セッションの Claude Code 環境（メモリ / スキル / プラグイン / コネクタ / 設定）
実施日: 2026-07-05
凡例: 🔴 高（機能不全・要修正） / 🟠 中（整理推奨） / 🟡 低（任意）/ ✅ 本PRで対応済

---

## 0. サマリ

| 領域 | 状態 | 主要な負債 |
| --- | --- | --- |
| メモリ (CLAUDE.md/README) | 🟠→✅ | リポジトリに説明・規約が皆無だった → `README.md` 追加 |
| スキル | 🔴→✅ | 唯一のスキルが**無効な形式**で読み込み不能だった → 正しい `SKILL.md` 化 |
| プラグイン | 🟡 | 0件（負債ではないが配布導線なし） |
| コネクタ (MCP) | 🟠 | 9件中3件が休眠（未認証・チャット無効） |
| 設定 (settings.json) | 🟡 | ユーザー設定なし（権限許可リスト未整備） |
| ブランチ | 🟠 | 同一起点から派生した重複ブランチが2本 |
| コンテンツ正確性 | 🟠→✅ | 出典なしの断定的数値 → 目安表記＋出典を付与 |

---

## 1. スキル負債（最重要）

### 1.1 🔴✅ 唯一のスキルが「無効な形式」で読み込まれない
- **事象:** リポジトリ唯一の資産 `ai-agent-architect.md` は**リポジトリ直下のフラットな Markdown**であり、Agent Skill として認識されない。
- **根拠:** スキルは「`<name>/SKILL.md`（YAML frontmatter に `name` と `description`）」というディレクトリ構成が必須（Anthropic 公式仕様）。フラットな `*.md` は Claude Code にも claude.ai スキルとしても登録されない。実際、本アカウントの有効スキル一覧（11件）にも本ファイルは存在せず、**完全に孤立（dead）**していた。
- **付随:** 本文冒頭に claude.ai スキルビルダー UI の定型文（「スキル名:／説明文:／アイコン案:／スターター3選」）がそのまま貼られており、スキル指示ではなくメタ情報だった。frontmatter が無いため**起動条件（trigger）も定義されていなかった**。
- **対応（本PR）:** `ai-agent-architect/SKILL.md` へ変換。`name`/`description`（何を・いつ使うか）を付与、UI定型文を除去、`references/` に出典を分離。旧フラットファイルは削除。

### 1.2 🟠 重複ブランチ
- **事象:** `claude/code-debt-inventory-plk811` と `claude/ai-agent-knowledge-base-5kn6t` は**同一のフラットファイル・コミット `5906c90` を起点**とし、着手時点では内容が一致していた（単一の正がない状態）。現在は本ブランチ（`code-debt-inventory-plk811`）が本修正コミットを上乗せしており、`ai-agent-knowledge-base-5kn6t` は起点のまま。
- **推奨:** 本修正を保持する `code-debt-inventory-plk811` をマージして一本化し、もう一方はマージ後に削除する。

### 1.3 🟠✅ コンテンツ正確性（出典なしの断定数値）
- **事象:** 知識ベースが具体的数値を事実として断定（例: 「67%」「81%向上」「20%短縮」）。一部（McKinsey「Lilli」侵害: 約2時間・約4,650万メッセージ・システムプロンプト露出）は**実在の2026年3月事案として裏が取れた**が、他の百分率は出所が揺れる。
- **リスク:** 「専門家」を名乗るスキルが未検証の数値を伝播させ、意思決定を誤らせる。
- **対応（本PR）:** 裏が取れた事例には出典を付与。裏取り困難な数値は「解説用の目安（要一次検証）」と明記し、`references/sources.md` に根拠の強さ（🟢🟡⚪）付きで整理。

---

## 2. メモリ負債（CLAUDE.md / ドキュメント）

### 2.1 🟠✅ リポジトリに説明・規約が皆無
- **事象:** `README.md` も `CLAUDE.md` も無し。`skills` という名前のリポジトリなのに、スキルの置き方・命名規約・構成の記述がゼロ。新規セッションやコントリビュータが構造を再発見するコストが発生。
- **対応（本PR）:** `README.md` を追加。ディレクトリ構成・`SKILL.md` frontmatter 規約（`name` の予約語制限含む）・収録スキル一覧・メンテ方針を記載。実質的に本リポジトリのメモリとして機能。

> 注: ユーザー/グローバルの `~/.claude/CLAUDE.md` は存在せず（負債ではない）。ハーネス管理の hook（`session-start-*`, `stop-hook-*`）は正規のもので棚卸し対象外。

---

## 3. プラグイン負債

### 3.1 🟡 プラグイン 0件
- **事象:** 有効プラグインなし（`ListPlugins` → 空）。それ自体は負債ではない。
- **推奨（任意）:** 将来スキルが増えるなら、marketplace 形式のプラグインとして束ねると配布・バージョン管理が容易。現時点では過剰。
- **更新（2026-07-07）:** `.claude-plugin/`（marketplace.json + plugin.json）を整備し、`/plugin marketplace add GHshimon/skills` での1コマンド配布に対応済み。

---

## 4. コネクタ（MCP）負債

有効化済みコネクタ 9件のうち **3件が休眠**。スキル自身の「最小権限」原則にも反する。

| コネクタ | 状態 | 判定 |
| --- | --- | --- |
| Canva / Figma / Gmail / Google Drive / Notion / Slack | connected・チャット有効 | ✅ 稼働中 |
| **Google Calendar** | installState=unknown・チャット無効 | 🟠 休眠 |
| **Linear** | installState=unknown・チャット無効 | 🟠 休眠 |
| **Vercel** | installState=unknown・チャット無効 | 🟠 休眠 |

- **リスク:** 未認証/未使用の接続は攻撃面と混乱の元。
- **推奨:** 使うなら認証してチャットで有効化、使わないなら削除。加えて、再現性が要るなら利用コネクタをリポジトリの `.mcp.json` に固定化（現在は全てアカウント単位でリポジトリに紐づかない）。

---

## 5. 設定 / ハーネス負債

### 5.1 🟡 ユーザー `settings.json` 不在（権限許可リスト未整備）
- **事象:** `~/.claude/settings.json` も プロジェクト `.claude/settings.json` も無し。権限は既定のまま。
- **推奨（任意）:** よく使う読み取り専用コマンドを許可リスト化すると権限プロンプトが減る（`/fewer-permission-prompts`）。小規模な現状では必須ではない。

### 5.2 ✅ プラグインのスコープ整理（2026-07-07 対応）
- **事象:** 13プラグインが全プロジェクトでグローバル有効。常駐 description を実測した結果、
  vercel 単体で**約110KB**（スキル36個）＋毎セッションの SessionStart 注入フックがあり、
  TouchDesigner 等の無関係なセッションのコンテキストを恒常的に圧迫していた。
- **対応:** 実測データに基づき重い4つだけを降格（残り9個は各8KB未満で汎用性が高いため現状維持）:
  - `vercel`（110KB）→ グローバル無効。Next.js の `プロフィールページ/site-nextjs` のみ project 有効
  - `pr-review-toolkit`（10.7KB のエージェント定義6個）→ 無効（レビューは Opus 独立レビュー方式で代替済み）
  - `plugin-dev`（6.9KB）→ 本リポジトリ（skills）のみ project 有効
  - `mcp-server-dev`（1.4KB）→ 無効（MCP開発時に再有効化）
- **効果:** 一般セッションの常駐プラグイン description が約133KB → 約14KB（**約9割削減**）。
- **復元:** `~/.claude/backups/settings.json.pre-plugin-scope-20260707` にバックアップあり。
  再有効化は対象プロジェクトの `.claude/settings.json` に `"enabledPlugins": {"<plugin>@<marketplace>": true}` を追記。

---

## 6. 対応済み / 残タスク

**本PRで対応済（✅）**
1. スキルを有効な `ai-agent-architect/SKILL.md` 形式へ修正（最重要 1.1）
2. 出典を `references/sources.md` に分離し、断定数値を目安表記化（1.3）
3. リポジトリ `README.md`（メモリ/規約）を追加（2.1）

**推奨（本PR対象外・要判断）**
- 重複ブランチの集約（1.2）
- 休眠コネクタ3件の認証 or 削除（4）
- 権限許可リストの整備（5.1）

---

## 7. スキル完成度ベースライン（2026-07-07 初適用）

[`eval-rubric.md`](skills/skill-foundry/references/eval-rubric.md) の10軸を配備済み3スキルに初適用した基準線。
軸: ①起動精度 ②形式適合 ③実行者適合 ④入出力契約 ⑤手順の実行可能性 ⑥自己完結性 ⑦接地 ⑧検証と完了条件 ⑨簡潔性 ⑩保守性

| スキル | ①〜⑩ | 合計 | 判定 |
| --- | --- | --- | --- |
| `skill-foundry` | 2,2,2,2,2,2,1,2,2,2 | 19 | 配備可 ※直近の編集者による自己採点のため参考値 |
| `dx-consult-agent` | 2,2,1,1,2,2,2,1,2,1 | 16 | 配備可 |
| `ai-agent-architect` | 2,2,1,1,2,2,1,0,2,1 | 14 | **配備不可**（⑧完了条件が0点） |

### 改善対応後の再採点（2026-07-07 同日対応 ✅）

| スキル | ①〜⑩ | 合計 | 前回比 | 判定 |
| --- | --- | --- | --- | --- |
| `skill-foundry` | 2,2,2,2,2,2,2,2,2,2 | 20 | +1（⑦: sources.md 追加） | 配備可 ※自己採点のため参考値 |
| `dx-consult-agent` | 2,2,2,2,2,2,2,2,2,1 | 19 | +3（③想定実行者 / ④入力契約 / ⑧提出前セルフチェック） | 配備可 |
| `ai-agent-architect` | 2,2,2,2,2,2,2,2,2,1 | 19 | +5（③想定実行者 / ④⑧完了条件 / ⑦根拠強度マーカー） | **配備可** |

- 残る ⑩=1（保守性）は「失敗例セクションの運用起点更新の実績がない」ためで、追記では解消しない（運用で自然解消・経過観察）。

### 低スコア軸の内訳と改善アクション

- **`ai-agent-architect` ⑧=0（最優先）:** 完了条件セクションが存在しない（リポジトリ規約「完了条件のないセクションを作らない」にも抵触）。→ 助言タスクの終了基準（例:「依頼された観点にセクション1〜5の該当知見を紐づけて提示したら終了」）を追記する
- **`ai-agent-architect` ⑦=1:** 「目安」表記と sources.md はあるが、断定ごとの根拠強度マーカー（🟢🟡⚪）が本文にない。→ dx-consult-agent と同形式に揃える
- **`dx-consult-agent` ④=1 / ⑧=1:** 出力（依頼種別ごとの成果物）は明確だが入力契約がなく、機械的チェックも未定義。→ 診断に最低限必要な入力（業務フロー資料等）を宣言する
- **両者 ③=1:** プロファイル制度（Manager/Worker）導入前の作成のため想定実行者が未記載。→ 冒頭に1行追記（両者とも Manager プロファイル相当）
- **共通 ⑩=1:** 失敗例セクションはあるが運用起点の更新実績がまだない（新規スキルのため経過観察）
- **`skill-foundry` ⑦=1:** 自身の `references/sources.md` がなく、根拠は README 経由の間接参照。→ sources.md を追加するか README の出典を明示参照する

※ ルーブリックの規定（配備判定は別の実行者が行う）により、skill-foundry の行は次回 Opus サブエージェント等による独立採点で確定させる。
